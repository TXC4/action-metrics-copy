The url variable in server.js must be filled with a link to a PropertyWare API url for a report with the attributes:
    1. Work Order #
    2. Status (Open or Closed)
    3. Date Created
    4. Date Completed

Statistics will then be generated with the default dates found in the GET request in server.js
Statistics will be filtered by the data the user inserts into the post request 